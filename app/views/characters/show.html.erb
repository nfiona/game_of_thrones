<h1> <%= @character.name %> </h1>
    <div class="character">
        <img src="<%= @character.img_url %>"/>
    </div>
    <h3> "<%= @character.quote %>" </h3>
    <h4> <%= link_to "Edit Character", edit_house_character_path(@house,@character) %> | <%= link_to "Back to House", house_path(@house) %> </h3>
